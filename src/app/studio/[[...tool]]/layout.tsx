export const metadata = {
  title: 'CoverPath Studio',
  robots: 'noindex',
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children;
}
